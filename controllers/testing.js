export const getTesting = (req, res) => { 
  return res.send('testing is working from testing controller');
}

export const postTesting = (req, res) => { 
  return res.send('post testing is working from testing controller');
}